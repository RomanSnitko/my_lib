# my_lib
library using modern CMake (static/shared builds, options, custom targets, generated headers), packaged it as an installable and relocatable CMake package, integrated GoogleTest for unit testing, and wrapped the library with Conan including an external dependency on zlib for file compression
